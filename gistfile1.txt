import 'package:flutter/material.dart';

void main() {
  runApp(DigitalDinerApp());
}

class DigitalDinerApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: InputScreen(), debugShowCheckedModeBanner: false);
  }
}


// SCREEN 1 — User Input

class InputScreen extends StatelessWidget {
  final TextEditingController inputController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFD9F5C4),
      appBar: AppBar(
        backgroundColor: Color(0xFFD9F5C4),
        elevation: 0,
        actions: [
          IconButton(
            icon: Icon(Icons.favorite, color: Colors.red),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => FavoritesScreen(favorites: []),
                ),
              );
            },
          ),
          IconButton(
            icon: Icon(Icons.person, color: Colors.black),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => ProfileScreen(),
                ),
              );
            },
          ),
          IconButton(
            icon: Icon(Icons.settings, color: Colors.black),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => SettingsScreen(),
                ),
              );
            },
          ),
        ],
      ),
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.all(25),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(
                "Digital Diner",
                style: TextStyle(
                  fontSize: 34,
                  fontWeight: FontWeight.bold,
                  color: Colors.green[800],
                ),
              ),
              SizedBox(height: 40),
              TextField(
                controller: inputController,
                decoration: InputDecoration(
                  hintText: "I want to eat...",
                  filled: true,
                  fillColor: Color(0xFFAED7C1),
                  border: OutlineInputBorder(
                    borderRadius: BorderRadius.circular(12),
                  ),
                ),
              ),
              SizedBox(height: 20),
              SizedBox(
                width: double.infinity,
                child: ElevatedButton(
                  style: ElevatedButton.styleFrom(
                    backgroundColor: Color(0xFF5F7F45),
                  ),
                  child: Text("Search"),
                  onPressed: () {
                    Navigator.push(
                      context,
                      MaterialPageRoute(
                        builder: (context) => SuggestionsScreen(
                          userInput: inputController.text,
                        ),
                      ),
                    );
                  },
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}


// SCREEN 2 — Suggestions

class SuggestionsScreen extends StatefulWidget {
  final String userInput;

  SuggestionsScreen({required this.userInput});

  @override
  _SuggestionsScreenState createState() => _SuggestionsScreenState();
}

class _SuggestionsScreenState extends State<SuggestionsScreen> {
  late TextEditingController searchController; // initialized with user input
  List<String> suggestions = [
    "Thai Food",
    "BBQ",
    "Asian Fusion",
    "Sushi Bar",
    "Mexican Tacos",
    "Vegan Cafe",
    "Italian",
    "Mediterranean",
    "Indian",
    "Korean BBQ",
    "Chinese",
    "Burger Joint",
    "Dessert Cafe",
  ];
  List<String> favorites = [];

  @override
  void initState() {
    super.initState();
    searchController = TextEditingController(text: widget.userInput);
  }

  @override
  void dispose() {
    searchController.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFD9F5C4),
      appBar: AppBar(
        backgroundColor: Color(0xFFD9F5C4),
        elevation: 1,
        title: Text("Suggestions", style: TextStyle(color: Colors.black)),
        iconTheme: IconThemeData(color: Colors.black),
        actions: [
          IconButton(
            icon: Icon(Icons.favorite, color: Colors.red),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) =>
                      FavoritesScreen(favorites: favorites),
                ),
              );
            },
          ),
          IconButton(
            icon: Icon(Icons.person, color: Colors.black),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => ProfileScreen(),
                ),
              );
            },
          ),
          IconButton(
            icon: Icon(Icons.settings, color: Colors.black),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (context) => SettingsScreen(),
                ),
              );
            },
          ),
        ],
      ),
      body: Padding(
        padding: const EdgeInsets.all(25),
        child: Column(
          children: [
            TextField(
              controller: searchController,
              decoration: InputDecoration(
                hintText: "Search again...",
                filled: true,
                fillColor: Color(0xFFAED7C1),
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
              ),
            ),
            SizedBox(height: 15),
            SizedBox(
              width: double.infinity,
              child: ElevatedButton(
                style: ElevatedButton.styleFrom(
                  backgroundColor: Color(0xFF5F7F45),
                ),
                child: Text("Search"),
                onPressed: () {
                  setState(() {
                    suggestions.shuffle();
                  });
                },
              ),
            ),
            SizedBox(height: 20),
            Expanded(
              child: ListView.builder(
                itemCount: suggestions.length,
                itemBuilder: (context, index) {
                  final suggestion = suggestions[index];
                  final isFav = favorites.contains(suggestion);
                  return Container(
                    margin: EdgeInsets.only(bottom: 12),
                    padding: EdgeInsets.all(18),
                    decoration: BoxDecoration(
                      color: Color(0xFFAED7C1),
                      borderRadius: BorderRadius.circular(14),
                    ),
                    child: Row(
                      mainAxisAlignment: MainAxisAlignment.spaceBetween,
                      children: [
                        Text(
                          suggestion,
                          style: TextStyle(
                            fontSize: 18,
                            fontWeight: FontWeight.w500,
                            color: Colors.green[900],
                          ),
                        ),
                        IconButton(
                          icon: Icon(
                            isFav ? Icons.favorite : Icons.favorite_border,
                            color: Colors.red,
                          ),
                          onPressed: () {
                            setState(() {
                              isFav
                                  ? favorites.remove(suggestion)
                                  : favorites.add(suggestion);
                            });
                          },
                        ),
                      ],
                    ),
                  );
                },
              ),
            ),
          ],
        ),
      ),
    );
  }
}

// SCREEN 3 — Favorites (TWO SECTIONS)

class FavoritesScreen extends StatelessWidget {
  final List<String> favorites;

  FavoritesScreen({required this.favorites});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFD9F5C4),
      appBar: AppBar(
        backgroundColor: Color(0xFFD9F5C4),
        elevation: 1,
        title: Text("Favorites", style: TextStyle(color: Colors.black)),
        iconTheme: IconThemeData(color: Colors.black),
      ),
      body: Padding(
        padding: const EdgeInsets.all(25),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(
              "Liked Restaurants",
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
                color: Colors.green[900],
              ),
            ),
            Container(
              width: double.infinity,
              padding: EdgeInsets.all(18),
              margin: EdgeInsets.only(top: 12, bottom: 30),
              decoration: BoxDecoration(
                color: Color(0xFFAED7C1),
                borderRadius: BorderRadius.circular(14),
              ),
              child: Text(
                "No liked restaurants yet",
                style: TextStyle(
                  fontSize: 16,
                  color: Colors.green[900],
                ),
              ),
            ),
            Text(
              "Liked Categories",
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
                color: Colors.green[900],
              ),
            ),
            SizedBox(height: 15),
            Expanded(
              child: favorites.isEmpty
                  ? Center(
                      child: Text(
                        "No liked categories yet",
                        style: TextStyle(
                          fontSize: 16,
                          color: Colors.green[900],
                        ),
                      ),
                    )
                  : ListView.builder(
                      itemCount: favorites.length,
                      itemBuilder: (context, index) {
                        return Container(
                          margin: EdgeInsets.only(bottom: 12),
                          padding: EdgeInsets.all(18),
                          decoration: BoxDecoration(
                            color: Color(0xFFAED7C1),
                            borderRadius: BorderRadius.circular(14),
                          ),
                          child: Text(
                            favorites[index],
                            style: TextStyle(
                              fontSize: 18,
                              fontWeight: FontWeight.w500,
                              color: Colors.green[900],
                            ),
                          ),
                        );
                      },
                    ),
            ),
          ],
        ),
      ),
    );
  }
}

// SIMPLE PROFILE SCREEN

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFD9F5C4),
      appBar: AppBar(
        backgroundColor: Color(0xFFD9F5C4),
        elevation: 1,
        title: Text("Profile", style: TextStyle(color: Colors.black)),
        iconTheme: IconThemeData(color: Colors.black),
      ),
      body: Center(
        child: Text(
          "User Profile",
          style: TextStyle(
            fontSize: 22,
            color: Colors.green[900],
          ),
        ),
      ),
    );
  }
}

// SIMPLE SETTINGS SCREEN

class SettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFD9F5C4),
      appBar: AppBar(
        backgroundColor: Color(0xFFD9F5C4),
        elevation: 1,
        title: Text("Settings", style: TextStyle(color: Colors.black)),
        iconTheme: IconThemeData(color: Colors.black),
      ),
      body: Center(
        child: Text(
          "Settings Page",
          style: TextStyle(
            fontSize: 22,
            color: Colors.green[900],
          ),
        ),
      ),
    );
  }
}
